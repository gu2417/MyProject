# 서버 모듈

| 파일 | 책임 |
|------|------|
| `server_main.c` | 서버 소켓 초기화, 연결 수락, 메인 루프 |
| `request_handler.c` | 요청 메시지 파싱, 명령별 처리 함수 호출, 응답 생성 |

## 서버 모듈 규칙

- 서버는 시작 시 `SMU_Students.txt` 를 읽어 연결 리스트를 초기화한다.
- 서버는 로그인, 사용자 관리, 학생정보 관리 요청을 처리한다.
- 서버는 매일 01:00 `SMU_Students.txt` 저장을 수행한다.
